const BASE = "https://agile-octopus.datafury.io";
const urls = [
    "",
    "about/",
    "about/agile-octopus",
    "about/plunge-pricing",
];

for (const url of urls) {
    console.log(`${BASE}/${url}`);
}